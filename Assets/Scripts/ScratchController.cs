using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Writing : MonoBehaviour
{
    public string scratch;
    public GameObject inputField;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /*Click anywhere on Scratch paper to type
     - When click, input field appears
     - When click and previous input field is empty, delete input field
     - Can move scratch Paper anywhere and change size
     ================================================================
     Stretch Goals
     - When having trouble, can check if thew math on scratch paper is correct
     -*/

}
